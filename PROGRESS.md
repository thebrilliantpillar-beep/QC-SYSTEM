# PROGRESS.md

이 파일은 "지금까지 뭘 했고, 다음에 뭘 할지"를 기록하는 곳이다. CLAUDE.md의 도메인 지식·패턴과 달리 **매번 갱신되는 내용**이므로 별도 파일로 분리했다.

새 세션 시작 시 CLAUDE.md와 함께 이 파일도 먼저 읽는다. 작업을 마칠 때마다 "최근 작업 이력"에 짧게 한 줄(또는 몇 줄) 추가하고, 오래된 항목은 주기적으로 정리(요약 후 삭제)한다 — 무한히 쌓아두지 않는다.

---

## 최근 작업 이력 (최신순)

- **2026-09-16 (추가4)**: "개선요청서" 신규 문서 서브시스템 추가 — 부적합통보서(NCR)
  보다 가벼운/러프한 단계의 협력사 문서. 사용자가 실제 회사 양식(엑셀)을 첨부해서
  그대로 `improvement_request_template.xlsx`로 반영. planner(NCR 구조 조사)→확인
  질문 3개(전부 "가장 단순한 안"으로 확정: 서명 없이 이름만/작성자가 바로 발송하는
  1단계/성적서연결+독립작성 둘 다 게이트 없이)→developer→quality-watcher 순서로
  진행. 새 테이블 2개(`improvement_requests`/`improvement_request_photos`), 새 권한
  `improvement`, 라우트 9개, 화면 4개 신규(목록/성적서연결작성/독립작성/상세),
  `quality_report()`에 NCR과 병렬로 집계 추가(별도 집계함수 안 만듦, 8-2-6절 원칙).
  developer가 실제 Excel COM까지 동원해서 사진 배치 검증했다고 보고했는데,
  quality-watcher가 "이 템플릿 기본 글꼴이 Calibri가 맞는지 확인 안 됨"이라고
  확인필요로 남긴 걸 직접 COM으로 재실측해서 **진짜 결함으로 확정**(맑은고딕
  MDW=8인데 옛 Calibri 계수 7*9525를 복붙해서 사진폭이 87.6%만 채워짐, 출고
  이력 엑셀 때와 똑같은 유형의 버그 세 번째 재발) — `_excel_col_width_to_emu()`로
  교체해서 재측정으로 완전히 고침(CLAUDE.md 7-4-1절에 세 번째 사례로 기록,
  "새 사진배치 함수 짤 때 7*9525 그냥 복붙하지 말 것"을 명시적 경고로 추가).
  아직 커밋 전 — "출고 계획외 항목 차단"(아래 정정 항목) 작업과 함께 한 번에
  커밋·배포 예정(사용자 요청).
- **2026-09-16 (추가3)**: 출고 스캔에 "차수 계획에 안 맞는 QR/S/N은 항목 추가 자체를
  막고, 그런 항목이 남아있으면 출고 확인 처리도 막는" 하드 블로킹 규칙 추가(사용자
  명시적 요청 — CLAUDE.md 21절의 "미등록/중복/계획외 전부 경고만" 원칙을 "계획외"
  하나에 한해 뒤집음, 24절에 기록). `db.outbound_batch_has_unplanned_items()` 신설
  (단건 조회용), `list_outbound_batches()`엔 SQL 서브쿼리로 병행(목록 N+1 방지).
  서버(`outbound_item_add`/`outbound_batch_confirm`)와 클라이언트(제출 전 선제
  차단) 이중 방어. 출고 스캔/출고 이력 두 화면이 `outbound_batch_confirm` 라우트
  하나를 공유해서 한 곳만 게이트하면 양쪽 다 자동 반영됨을 재확인. Flask
  test_client로 계획외 추가 거부/계획 일치 시 정상 추가/확인 처리 차단·허용/
  자유등록 배치는 영향 없음까지 실제 시나리오 6개 검증, quality-watcher 통과.
- **2026-09-16 (추가2)**: 출고 스캔 품질확인 UI 재배치 — 실사용 피드백("입력할 때
  스크롤이 너무 불편하다")에 따라 5개 품질확인항목+전체판정 미리보기를 "저장된
  항목" 표에서 "새 항목 스캔·입력" 카드 안(제품명/모델명 바로 다음)으로 옮김.
  판정 우선순위(FAIL>SPECIAL>PASS)를 `computeAutoResultClient()`라는 JS 순수함수로
  미러링해서 항목 생성 전에도 클라이언트에서 즉시 미리보기 계산(서버 왕복 없음,
  CLAUDE.md 8-1절 예외로 명시). "저장된 항목" 표는 품질확인/판정 열을 하나로 합쳐
  판정 배지만 기본 노출, "세부 ▾" 클릭 시 펼쳐지는 방식으로 압축(판정 번복 기능
  유지). planner 스펙 2건(응답 타이밍차로 초안 2개 생성됨, 실제 구현은 메인 세션이
  직접 진행) 참고해 구현 후 quality-watcher 검증 통과. 헤드리스 크롬의
  `--window-size`가 이 환경에서 신뢰 불가능한 걸 발견(요청 390px여도 실제 뷰포트
  500~518px로 렌더링됨, 원인 미상) — iframe으로 정확한 폭을 강제하는 우회법 확립,
  버튼 클릭까지 실제 시뮬레이션해서 판정 미리보기 정확성 확인(다음에 좁은 폭
  헤드리스 검증할 때 이 iframe 트릭을 먼저 쓸 것).
- **2026-09-16 (정정)**: 출고 품질확인 전체판정 우선순위를 사용자가 직접 뒤집었다.
  "Special이 있어도 Fail이 들어가면 무조건 최종 판결은 Fail"이라고 명시적으로 확정 —
  즉 **FAIL이 하나라도 있으면 무조건 FAIL(SPECIAL이 섞여 있어도) → 그 외 SPECIAL
  있으면 SPECIAL → 전부 PASS면 PASS**로 바뀜(`database.py`의
  `compute_outbound_item_auto_result()`). 바로 아래 추가12 항목에 적힌
  "SPECIAL>FAIL>PASS"(실제 회사 엑셀 서식 예시 데이터에서 역산한 순서)는 **이제
  틀린 정보** — 사용자가 그 예시와 반대로 직접 정정했다. 코드 docstring에는
  이 정정 배경을 남겨뒀다. 혹시 나중에 또 "엑셀 예시엔 SPECIAL이 이기던데?"라는
  질문이 나오면, 이건 예시 데이터보다 사용자의 명시적 정정이 우선한다는 걸 알아둘 것.
- **2026-09-15 (추가12, 커밋 `457c630`)**: 출고 스캔에 실제 회사 "출고 내역서"- **2026-09-15 (추가12, 커밋 `457c630`)**: 출고 스캔에 실제 회사 "출고 내역서"
  서식(11열)을 반영해 5개 품질확인항목(체결상태/QR부착/간지포장/RST단자/부속품,
  각 PASS/FAIL/SPECIAL 터치 3버튼) + 전체판정 자동계산(우선순위 SPECIAL>FAIL>PASS,
  실제 서식 예시 데이터로 확정) + 수동 오버라이드(판정 번복) 추가. 출고 이력 엑셀도
  11열로 재작성, 사진은 비율 무시하고 셀 꽉 채움(`_place_photos_in_area`에 `stretch`
  파라미터 추가, TwoCellAnchor 방식). planner→developer→designer→quality-watcher(2회)
  순서로 진행. 과정에서 메인 세션이 직접 잡은 것 2가지: (1) 품질확인 라벨에서 엑셀
  원본의 "(가대 다리, 탱크 다리, 네마)" 상세문구가 빠져있던 것 발견해 복원 + 라벨이
  90px로 잘려 보이던 CSS 수정, (2) **엑셀 사진이 셀 폭의 약 90%만 채우던 버그** —
  quality-watcher의 "폰트 미확인" 지적을 openpyxl로 직접 확인해서 일단 반증했으나,
  Python pywin32로 실제 Excel COM을 열어 셀/도형 크기를 직접 측정(`Shape.Width`
  vs `Range.Width`)해서 진짜 원인(이 워크북의 실제 MDW가 7이 아니라 8 — CLAUDE.md
  7-4-1절과 동일 유형)을 하드 데이터로 확정, 기존 `_excel_col_width_to_emu()` 헬퍼로
  교체 후 재측정으로 완전 일치(여백 0.0pt) 확인. **"코드 리뷰의 이론적 지적이 실측과
  다르면 실측이 이긴다"를 재확인한 사례** — CLAUDE.md 7-4-1절에 이 사례 추가 기록.
  412px 폭에서 전역 네비게이션까지 잘리는 현상을 발견했으나 이 기능과 무관한
  기존 전역 이슈로 확인(홈 화면도 동일 증상, 대조 스크린샷으로 검증) — 범위 밖으로
  남겨둠, 다음에 반응형 작업 우선순위 잡을 때 참고.

- **2026-09-15 (추가11, 커밋 `e498d80`)**: 로컬 컴퓨터 셧다운으로 중단됐던 작업을
  원격세션(/remote-control)으로 이어받음 — 출고 스캔 사진 업로드를 "항목 저장과
  같이 보내기"에서 "항목 먼저 저장 → 클라이언트에서 1280px로 미리 리사이즈
  (신규 `static/photo_resize.js`) 후 백그라운드 개별 업로드"로 전환, 업로드
  대기 슬롯/실패 재시도/이탈 경고 추가. `_save_ncr_photo()`에 `max_px/quality/
  optimize` 파라미터를 추가해서 출고 사진은 1280px/80%로 낮추되 **NCR(불량
  증거사진)은 기존 1600px/85% 그대로 유지**(사용자 확정 — 화질이 더 중요한
  문서라 낮추면 안 됨). 코드 리뷰만으로 끝내지 않고 격리된 임시 DB에 대해
  Flask test_client로 실제 항목생성→사진업로드→1280x853 실측 압축 확인→
  kind 구분→토글 방어까지 end-to-end로 실행 검증, NCR 경로도 별도로 1600px
  유지되는지 실제 호출까지 확인. quality-watcher 2회(1차 코드리뷰, 2차 실측
  결과 재검증) 통과. 헤드리스 크롬으로 브라우저 내 캔버스 리사이즈까지
  재현하려 했으나 이 세션 환경의 네트워크 제약(로컬 HTTP 서버에 크롬이 접속
  못 함)과 `--virtual-time-budget`+`--screenshot` 조합 미지원으로 실패 —
  대신 Node.js로 리사이즈 수식만 별도 검증(서버 실측 결과와 정확히 일치
  확인). origin/deploy 양쪽 원격에 push 완료.

- **2026-09-15 (추가10)**: 출고(🚚) 메뉴 6개 화면(차수 입력/S/N 발급/분류 규칙 관리/
  출고 스캔/출고 이력/QR 출력 이력) 전부에 일괄선택삭제 추가 + 신규 권한
  `outbound_delete` 신설(planner→developer→quality-watcher). 기존
  `templates/_admin_delete.html`의 `admin_delete_bar` 매크로(성적서 4개 화면이
  이미 쓰던 것)를 `show=('outbound_delete' in user_perms)`로 재사용 — 매크로
  자체는 수정 안 함. 매크로가 페이지당 1개 인스턴스만 지원하는 제약을 발견해서,
  분류 규칙(표 3개)은 `kind:code` 합성 키로, 출고 스캔(표 2개)은 두 표 모두에
  `data-admin-id`를 붙이는 방식으로 우회(CLAUDE.md 23절). 차수(배치) 삭제는
  차수입력/출고스캔/출고이력 3화면이 라우트 하나를 공유하고 cascade delete(항목→
  사진DB+파일→계획S/N→QR출력이력→배치) 처리, S/N발급이력은 배치와 무관해 안
  지워지는 것 확인. 확인완료(잠금)된 배치도 삭제는 가능(잠금≠삭제 방지). Flask
  test_client로 권한별 UI 노출/차단, end-to-end cascade 삭제(사진파일까지),
  finished_goods_serials 보존을 실제 검증. quality-watcher가 "3개 라우트가
  _resolve_return_to 안 씀" 지적 → 호출 화면이 하나뿐이라 의도된 설계로 확인(결함 아님).
- **2026-09-15 (추가9)**: 출고 확인 = 잠금 기능 구현(planner→developer→quality-watcher).
  "✅ 출고 확인"을 누르면 그 배치의 항목/사진/배치정보 수정이 즉시 잠기고(화면+서버
  양쪽), 확인자 본인 또는 admin 계정만 "↺ 확인 회수"로 풀 수 있다. 예전 설계문서
  확정사항("확인은 잠금 아님")을 사용자 요청으로 뒤집은 것 — CLAUDE.md 22절에 기록.
  회수는 새 세분화 권한이 아니라 신원(confirmed_by와 현재 사용자 표시이름 비교)으로
  게이트(`approve_revoke`와 다른 방식). "출고 스캔 — 차수 선택" 목록은 활성/완료
  두 섹션으로 분리, 완료 섹션은 기본 접힘(21절에서 도입한 접기 패턴 재사용).
  planner가 조사 중 "확인 액션이 한 곳뿐"이라던 기존 전제가 틀렸음을 발견(실제론
  outbound_scan.html/outbound_history.html 두 곳, 다행히 같은 백엔드라 영향 없음)
  — 사전 조사 노트도 핵심 파일은 직접 대조하는 습관이 유효함을 재확인.
  quality-watcher가 JS null참조 위험(확인된 배치에서 "새 항목 스캔" 카드가 없을 때
  관련 함수들이 방어 없이 호출되면 에러) 3건을 지적해 즉시 가드 추가. Flask
  test_client로 잠금 서버가드(수정 시도 시 302/409 확인, DB 값 불변 확인)와 신원별
  회수 권한(비확인자 거부, 확인자 본인/admin 통과)을 실제 임시 계정으로 검증,
  헤드리스 크롬으로 잠긴 화면·완료 섹션 렌더링 육안 확인 완료.
- **2026-09-15 (추가8)**: 출고 스캔 화면(`outbound_scan.html`) 구역 순서를 "새 항목
  스캔·입력 → 저장된 항목 → 출고 정보 → 계획 대비 진행상황"으로 재배치(사용자 요청,
  실제 스캔 작업이 가장 먼저 눈에 띄게). "계획 대비 진행상황"은 `<details class="sec">`
  접기/펼치기 구조로 바꾸고 기존 공용 스크립트 `static/collapsible_sections.js`
  (`CollapsibleSections.init`)를 재사용(daily_status.html/defect_history.html과
  같은 패턴, 8-1절 원칙). quality-watcher 검증 통과(중복 ID 없음, JS 참조 무결,
  Jinja 태그 짝 정상). 이어서 "출고 확인 처리 후 수정 잠금 + 완료 항목 섹션 분리 +
  확인자/관리자만 회수 가능" 기능은 planner에게 스펙 작성 요청(진행 중).
- **2026-09-15 (추가7, 중요)**: 사용자가 "엑셀 화면엔 절취선이 보이는데 인쇄(PDF)엔
  왜 가려지냐"고 문의 → 실제 원인은 QR 이미지가 지정한 3cm가 아니라 **셀 전체를
  꽉 채워서** 위아래 절취선을 덮는 버그였고, 이게 **LibreOffice로는 재현이 안 되고
  진짜 윈도우 엑셀에서만 나타나는** 문제였다(PDF 메타데이터로 "Microsoft: Print To
  PDF" 확인). 이 세션이 그동안 LibreOffice 변환으로 여러 번 "실측 검증"했다고
  믿었던 게 이미지 배치에 한해서는 불충분했다는 뜻 — 중요한 방법론적 교훈이라
  CLAUDE.md 7-4-2절/11절에 크게 기록해둠. 이 PC에 실제 Excel이 설치돼 있어
  PowerShell+COM 자동화(`Excel.Application` → `ExportAsFixedFormat`)로 직접
  재현·검증했고, `pymupdf`로 PDF를 고배율(4x) 렌더링해서 셀 경계와 이미지 경계를
  픽셀 단위로 대조하는 방법도 이번에 확립. 원인은 openpyxl이 `OneCellAnchor` 그림의
  `spPr`에 `xfrm`을 안 써주는데 실제 엑셀은 이게 없으면 `ext` 크기를 무시하고
  셀 전체로 늘리는 것으로 추정 — `build_outbound_qr_labels_excel()`의 QR 이미지
  배치를 `OneCellAnchor`+`ext`에서 `TwoCellAnchor(editAs="oneCell")`+`from`/`to`
  좌표 직접 지정 방식으로 교체해서 해결(Excel COM+LibreOffice 둘 다 정상 확인).
  `_insert_logo()`/서명 스탬프/`_place_photos_in_area()`는 여전히 OneCellAnchor라
  같은 위험이 잠재해있음 — 다음에 이 함수들을 고칠 일이 있으면 실제 엑셀로도
  검증할 것(미리 전부 바꾸진 않음, 과설계 방지).
- **2026-09-15 (추가6)**: QR 라벨 엑셀 2건 추가 조정. (1) 사용자가 실제 엑셀에서
  QR 이미지를 위 화살표로 6번 옮긴 위치로 재배치 요청 → 예전에 실측했던 "화살표
  한 번 이동거리"(81644/15 EMU, 오른쪽 15번 이동 실측값을 역산) 상수를 세로에도
  그대로 적용해서 6칸만큼 위로 뺌(가로/세로 이동거리가 같다는 가정, 화면픽셀 기준
  고정거리라 방향 무관). (2) "모든 시트 인쇄설정 A4/세로/머리글:시트이름" 요청 →
  QR 라벨 엑셀에만 적용하는 것으로 범위 확인(성적서/NCR/출고이력 등은 이미 각자
  맞는 설정이 있거나 물리적 크기 정확도가 중요해서 제외). 사용자가 실제 인쇄까지
  해보고 완성한 참고 파일(page_setup: A4·세로·고정배율 95%·머리글 "&A" 자동필드)을
  받아 그대로 옮김 — fit-to-page 대신 고정배율만 쓰는 방식이라 CLAUDE.md 7-5절에
  의도된 예외로 기록. 예전에 겪었던 "세로용지 2세트 배치 시 페이지 갈라짐" 버그가
  이 고정배율 95% 설정으로는 재발하지 않는지 LibreOffice PDF 변환으로 4개/15개
  항목 두 케이스 다 확인(안 갈라짐, 정상).
- **2026-09-15 (추가5)**: 출고 관리 사진 카테고리화(인디케이터/본체) + 관련 개선
  6건 구현. planner→developer→quality-watcher 순서로 진행(21절 참고). 주요 내용:
  (1) 사진을 인디케이터/본체 두 종류로 분리(`outbound_item_photos.kind` 컬럼).
  (2) 본체사진 기능은 admin 계정만 켜고 끌 수 있는 전역 토글(`db.get_setting`),
  기본 꺼짐. (3) "계획 대비 진행상황"을 숫자 요약에서 S/N별 전체 리스트+상태
  배지(확인됨/사진 미비/미스캔)로 확장, 사진·S/N 미비 시 "확인됨"만 안 뜨고
  항목 추가 자체는 안 막음(기존 설계원칙 유지). (4) 저장된 항목에 사진 재업로드
  (추가) 기능, 새 항목 입력에 "다시 스캔" 버튼 추가. (5) 차수 입력 화면 계획
  S/N 표에 모델명 컬럼 추가. (6) 출고 이력 엑셀을 사용자 제공 실제 회사 서식
  (순번/S·N/제품명·모델명/인디케이터 사진/본체사진 5열, 이미지 임베드)으로 전면
  재작성 — 사진 배치 계산은 NCR 통보서와 `_place_photos_in_area()` 공용화.
  실측(LibreOffice PDF 변환)으로 진짜 버그 발견: fit-to-page 설정 누락으로 5번째
  열("본체사진"/"담당자")이 PDF에서 통째로 사라짐 → 즉시 수정(CLAUDE.md 7-5절에
  재발 사례로 기록). Flask test_client+헤드리스 크롬으로 스캔/차수/규칙 화면
  3개 시각 확인, 재업로드·서버측 토글 강제 기능 확인, NCR 통보서 사진 배치
  회귀 없음 확인.
- **2026-09-15 (추가4)**: QR 라벨 엑셀에서 QR 이미지가 행 높이(90.8pt)에 꽉 차서
  위아래 절취선(테두리)을 이미지가 덮어 안 보인다는 피드백 → QR 이미지 높이를
  고정 3cm로 줄이고(`QR_LABEL_QR_HEIGHT_CM`), 위쪽 절취선이 더 잘 보이게 세로
  중앙보다 6px 아래로 내림(`QR_LABEL_QR_DOWN_SHIFT_PX`). quality-watcher가 이
  6px를 그대로 더하면 이미지 아랫변이 행 경계를 20,570 EMU(~2px)만큼 넘어서
  오히려 아래쪽 절취선을 가릴 수 있다고 계산으로 지적 → `qr_row_off`를 행 경계
  (`row_h_emu - qr_size_emu`)로 clamp해서 아랫변이 절대 행을 못 넘게 고침.
  실측(LibreOffice PDF)으로 위아래 절취선 모두 보이는 것 확인.
- **2026-09-15 (추가3)**: QR 라벨 이미지가 셀 왼쪽으로 치우치는 문제의 진짜 원인을
  찾아 고침. 원인은 열너비(문자단위)→EMU 변환 상수가 Calibri 11pt 기준(MDW=7px)이었는데,
  이 워크북 실제 기본 글꼴은 "맑은 고딕" 11pt로 MDW가 8px이라 달랐던 것 — 사용자가
  실제 엑셀에서 이미지를 화살표로 15번 오른쪽으로 옮겨 눈으로 정중앙을 맞춘 값을
  역산해서 확인(오차 0.08px 이내로 거의 정확히 일치). ECMA-376 표준 열너비→픽셀
  변환 공식을 MDW=8로 구현한 `_excel_col_width_to_emu()` 헬퍼를 새로 만들어 기존
  Calibri식 상수를 대체함 — 이제 화살표로 수동 조정할 필요 없이 항상 정중앙에 옴.
  같은 김에 S/N 폰트를 18pt(shrink_to_fit)에서 12pt 고정+가로세로 정중앙 정렬로
  변경(사용자 확정, shrink_to_fit은 예외적으로 긴 S/N 대비 안전장치로 유지).
  quality-watcher가 `_build_ncr_photo_sheet()`(928줄)·`_append_ncr_photos()`(1025줄)도
  똑같은 Calibri 기준 MDW=7 상수를 쓰고 있어 그 워크북도 기본 글꼴이 맑은 고딕이면
  같은 문제가 있을 수 있다고 발견함(고치라는 요청 없어 보류, 재발 시 이 노트부터
  볼 것 — `_excel_col_width_to_emu(mdw=8)`로 통합 고려).
- **2026-09-15 (추가2)**: QR 라벨 엑셀 열너비/행높이를 사용자가 자기 엑셀에서 직접
  측정한 값(S/N 텍스트 열 24.86, QR 열 16.86, 행높이 90.8pt — 전부 엑셀 열너비/
  행높이 설정 대화상자와 같은 단위라 변환 없이 그대로 대입)으로 고정. QR 이미지는
  `min(열너비 EMU, 행높이 EMU)`로 정사각형 크기를 정하고 OneCellAnchor 오프셋으로
  셀 안에 꽉 차게 중앙정렬(7-4절 패턴 재사용). 실측(LibreOffice PDF 변환)하다가
  진짜 버그 발견: 18pt 볼드 폰트로는 긴 S/N(23자 등)이 24.86 너비를 넘쳐서 QR
  이미지에 가려 텍스트가 잘려 보임 → S/N 셀 Alignment에 `shrink_to_fit=True` 추가로
  해결(자재마다 S/N 길이가 달라도 항상 한 줄에 다 보이게). quality-watcher 검증
  통과(단위/정렬옵션충돌/중복/호출부/8-1절·7-4절 원칙 전부 확인).
- **2026-09-15 (추가)**: 출고 관리 QR 라벨/차수 화면 사용자 피드백 3건 반영(커밋
  `a2228a1`). (1) "차수 입력" 메뉴가 항상 신규등록으로만 가서 이미 만든 차수를
  다시 열어 계획을 추가하거나 QR을 재출력할 방법이 없던 실사용 문제 발견 →
  "차수 목록" 화면 신규(`/outbound/round`, `outbound_scan_list()`와 동일 패턴),
  각 행에서 "차수/계획 수정"·"QR 라벨 출력" 바로 연결. (2) 출고 메뉴에서 "차수
  입력"을 맨 위로 이동. (3) QR 라벨 엑셀에 테두리 추가(참고 샘플과 통일, 절취선
  겸용). 이 작업 중 **LibreOffice 로컬 설치가 재설치 후에도 매번 새로 만드는
  격리 프로필(`-env:UserInstallation=`)에서만 `libpng error`로 깨지고, 기존
  프로필 재사용 시엔 정상 동작**하는 걸 발견 — `report_builder.py`의 실제 PDF
  변환은 매 호출마다 `tempfile.mkdtemp()`로 새 프로필을 쓰므로 이 로컬 환경에서
  잠재적으로 영향받을 수 있음(운영 서버 Render는 별도 환경이라 무관할 가능성
  높음, 원인 미조사 상태로 남겨둠 — 재발 시 이 노트부터 볼 것). 이 문제를 우회해서
  실제 xlsx→PDF 변환으로 QR 라벨 레이아웃을 직접 검증하다가 진짜 버그 2개를
  찾아 즉시 수정: ① 텍스트 칸이 좁고 가운데정렬이라 긴 S/N이 시트 왼쪽 경계
  밖으로 잘려 안 보이던 문제(왼쪽정렬+칸 확장으로 해결) ② 칸을 넓히니 한 줄
  2열쌍 배치 총폭이 세로용지를 넘어 페이지가 갈라지며 항목 순서가 뒤섞이던
  문제(가로용지+폭맞춤으로 해결). 코드리뷰만으론 못 잡았을 문제들이라
  CLAUDE.md 11절(시각적 완료선언 금지) 원칙이 다시 한번 유효함을 확인.
- **2026-09-15**: 출고 관리 기능 대폭 확장(커밋 `d09f492`) — (1) S/N 문자열 패턴
  (`CKMR...`=리클로저 본체, `CKCB...`=제어함)으로 제품명을 자동 판별하는 분류 규칙
  엔진 신설, 전압코드/접미사/P코드특수값 3개 매핑표를 화면에서 CRUD 가능("분류 규칙
  관리" 화면). 접미사는 긴 것부터 매칭해야 해서(HAT3가 H/HA보다 먼저) 정렬 순서가
  중요 — `get_classify_rules()`가 길이 내림차순 정렬. (2) S/N 발급을 입고등록과 같은
  붙여넣기 방식으로 전환(여러 줄 한번에 발급) + 발급이력 삭제 추가. (3) "차수 입력"
  화면 신설 — 차수번호+출고예정일+거래처와 함께, 이번 차수에 나갈 예정 S/N을 미리
  등록(계획/매니페스트, `outbound_planned_items`). (4) 출고 스캔 화면 전면 재작성 —
  수량칸 삭제, 차수 먼저 선택, S/N+모델명 자동입력, 계획 대비 진행상황 표시.
  **"리스트에 추가" 즉시 서버저장(AJAX)으로 아키텍처 변경** — 기존 "누적 후 일괄제출"
  방식(`_outbound_scan_submit`/`items_json`)을 완전히 폐기하고, 카메라 스트림이
  재시작되지 않도록 fetch+FormData로 즉시 POST하는 방식으로 바꿈("매번 임시저장
  필수" 요구사항 대응). (5) 종류별 QR 라벨 엑셀 출력 신규 — 카테고리별 시트 분리,
  시트당 S/N+QR이미지 2열쌍 반복 배치, 시트명 31자 제한/금지문자 처리, "QR 출력
  이력" 화면. (6) 출고 이력에 "출고 확인" 버튼(확인자+확인일시 기록, 확인 후에도
  수정 가능 — 잠금 아님). "미등록 S/N"/"타배치사용 S/N"/"계획외 항목" 세 경고는
  전부 진행을 막지 않고 경고만(원칙 통일). quality-watcher가 항목 수정 라우트에서
  수량칸 삭제 후에도 폼의 수량값을 그대로 저장하던 결함 발견, 기존 수량 보존하도록
  즉시 수정. 로컬 LibreOffice 설치의 `bootstrap.ini`가 손상돼 있어 xlsx→PDF 시각
  검증을 못 해서 xlsx 내부 QR PNG를 직접 추출해 육안 확인하는 것으로 대체(재설치
  권장, 급하지 않음). 브레인스토밍(설계 확정, 여러 차례 확인 질문) → planner
  (구현스펙 3단계로 보강) → developer → quality-watcher 순서로 진행. 설계/계획
  문서: `docs/superpowers/specs/2026-09-14-outbound-shipment-design.md`,
  `docs/superpowers/plans/2026-09-15-outbound-shipment-v2-plan.md`.
- **2026-09-14 (추가4)**: 출고(완제품 S/N·QR·사진) 관리 신규 기능 추가(커밋 `a07d7ec`) —
  IQC 입고검사와 완전히 별개인 새 하위시스템. 생산완료 직후 S/N 발급(QR생성+인쇄) →
  출고 시 QR스캔/수동입력으로 S/N 자동입력+제품명/수량+사진여러장을 배치 단위로
  리스트업 후 저장 → 배치별 엑셀출력. 새 테이블 4개(finished_goods_serials/
  outbound_batches/outbound_items/outbound_item_photos, 기존 테이블 FK 없이 느슨한
  연결), 새 권한 `outbound`, 새 메뉴 "출고". QR생성은 서버 `qrcode` 패키지, QR스캔은
  `html5-qrcode`(CDN), 사진첨부는 NCR의 `_save_ncr_photo()` 재사용. 미등록 S/N·이미
  다른 배치에 쓰인 S/N은 경고만 하고 진행 허용(사용자 확정 원칙), 저장 후에도 항목
  추가/수정/삭제 항상 가능(성적서처럼 안 잠김). 엑셀은 우선 기본 표 형태(사용자가
  실제 양식 파일 제공하면 NCR/성적서와 같은 템플릿-복사 방식으로 교체 예정).
  brainstorming 스킬로 설계 확정(여러 차례 확인 질문) → planner → developer →
  quality-watcher 순서로 진행. quality-watcher가 사진 서빙 라우트에 권한 데코레이터
  누락된 보안 결함 1건 발견해 즉시 수정. 설계/계획 문서:
  `docs/superpowers/specs/2026-09-14-outbound-shipment-design.md`,
  `docs/superpowers/plans/2026-09-14-outbound-shipment-plan.md`.
- **2026-09-14 (추가3)**: 검사 대기 목록(`/inspect/new`)에도 포함/제외 다중 단어 검색
  추가(커밋 `c1e5587`) — 자재관리/자재찾기/검사이력 등과 같은 패턴(`_include_exclude_clause()`
  재사용). 구현 중 quality-watcher가 실사용 버그 발견: 입고일 범위검색과 동시에 쓰면
  `_row_passes_search()` 재필터링 단계가 material/supplier/product/lot을 안 넘겨서
  포함/제외가 recv_date 하나만 놓고 재검사돼 이미 SQL로 통과한 행이 부당히 빠지던 문제 →
  해당 필드들을 같이 넘기도록 수정 후 재검증 통과.
- **2026-09-14 (추가2)**: 성적서 항목 판정 수동 오버라이드 기능 신규 추가(커밋 `01b0cc6`).
  배경: 성적서 #774(자재 605007P000)에서 AQL 1.5(Ac1/Re2) 허용범위 안이라 자동판정이
  "합격"으로 나왔는데, 사후에 NCR을 보내야 하는 상황이 생김 — 자동생성 로직이 "불합격"
  판정 항목만 상세내역을 채우다 보니 통보서 내용이 부실했던 게 발단.
  `inspection_items.result`(자동판정 원본)는 절대 안 건드리고 `inspection_item_overrides`
  별도 테이블에 오버라이드만 기록 → `effective_result()` 헬퍼로 NCR·상세화면·승인전
  검토화면·품질현황 대시보드(`quality_report`/`_lot_state`)에 반영. 승인된 성적서도
  오버라이드 허용하되 공식 xlsx/PDF/커스텀 성적서는 항상 원본 그대로(무결성 해시
  `compute_content_hash()`가 `overall_result`를 저장 컬럼 대신 원본 항목결과로 재계산하도록
  바꿔서 기존 승인건 해시가 안 깨지게 함 — 실제 승인건 6건으로 회귀검증 완료). 승인
  상태(합격승인/특채/불합격확정) 자체는 자동으로 안 바뀌고 승인회수→재결정 절차 유지.
  planner를 3차례 이어서 호출(권한/승인건 허용여부/전체판정 연동/대시보드 집계 연동을
  각각 사용자 확인받으며 설계 확장) → developer 구현 → quality-watcher 2회(기능+디자인
  수정본) → 헤드리스 크롬으로 실제 렌더링 확인 → designer가 오버라이드 정보 표시를
  좁은 표 열 텍스트 뭉침에서 기존 정보 팝오버 패턴(`_glossary.html` 방식)으로 정리.
  설계 전문: `docs/superpowers/plans/2026-09-14-item-result-manual-override.md`.
- **2026-09-14**: (1) 오늘 실적 집계(`/today`, 홈 화면 "오늘 현황")가 제출 시각
  (created_at) 기준이라 검사한 날과 제출한 날이 다르면 실적이 어긋나던 버그 수정 —
  `get_today_stats()`/`daily_status()` 모두 실제 검사한 날짜(inspect_date) 기준으로
  변경. (2) 포함/제외 단어 다중 검색 신규 — 검사이력·불량이력·부적합통보서·반품처리·
  과거입고이력(공유 `_list_search.html`) + 자재 관리 + 자재 찾기 3그룹에 적용.
  포함 단어 여러 개=OR(하나라도 있으면 통과), 제외 단어=OR(하나라도 걸리면 제외),
  자재 관리/자재 찾기는 자재번호+자재명만 대상(규격표기 등은 LEFT JOIN 다중행
  문제로 의도적 제외). `database.py`에 공용 SQL 헬퍼 `_include_exclude_clause()`
  신설 — 구현 중 LEFT JOIN NULL 컬럼에서 `NOT(NULL)` 3치논리로 미등록 자재가
  제외검색 때 부당하게 다 빠지는 실제 버그를 발견해 COALESCE로 방지(헬퍼에 내장).
  planner→developer→quality-watcher 순서로 진행, 헤드리스 Chrome 스크린샷으로
  3개 화면 실제 렌더링까지 확인 후 커밋(dc8df8b).
- **2026-09-10 (추가2)**: 전체 워크플로우 로직 감사(4개 영역 병렬 fork) → 실제 결함
  7건 + 설계변경 1건 발견 즉시 수정, quality-watcher 검증 통과(결함 0). 보안 구멍
  1건(`_can_make_final_decision()` 폴백이 'approve' 권한을 안 보던 버그, NCR 확인
  경로로 실제 뚫림 — 그 김에 NCR 확인은 최종결정권자 게이트 자체를 떼기로 사용자
  확정), BOM Lv 판별 로직 반대로 짜여있던 버그, 재검사 제출 시 draft 삭제 누락,
  도면번호 "P" 전체치환 버그, 판정이탈 로직 3중복 통합, 일괄승인 대리자 미검증,
  성적서 중복생성 경합조건(TOCTOU) 방어 추가. 상세는 CLAUDE.md 20절.
- **2026-09-10 (추가)**: git 안전장치(하드 차단) 신규 구축 — `~/.claude/hooks/git-guardrail.py`
  + 전역 `~/.claude/settings.json`의 `PreToolUse`(matcher: Bash) 훅. `git reset --hard`,
  `git push --force`(-with-lease 포함), `git clean -f*`, `git checkout .`/`git restore .`,
  `git branch -D`를 `permissionDecision:"deny"`로 하드 차단(자동모드 승인이어도 못 뚫음).
  전역 배치 이유: git 사고 방지는 iqc-app 한정이 아니라 일반적 위험이라서 — "QMS는
  포트폴리오라 지금 만들어두자"는 사용자 명시적 지시(2026-09-10)에 따라 구축.
  임시 테스트 저장소에서 `git reset --hard`가 실제로 막히고(파일 내용 그대로 보존
  확인) `git status`/`cat` 등 정상 명령은 그대로 통과하는 걸 라이브로 검증 완료.
  이 세션 전에 있었던 PreCompact(메모리 점검)·PostToolUse(진행기록 점검) 훅에 이어
  세 번째 안전장치.
- **2026-09-10**: (1) 통합BOM(28개 모델) Lv1~5 계층 정보 연동 신규 기능 — `material_bom_links`
  테이블, "자재 찾기"/"BOM 임포트" 화면(assembly_masters와는 별개, CLAUDE.md 18절). BOM 기준
  구매자재 573건을 신규등록/이름동기화, 키워드 기반 자동분류(※완제품/★완전포장제품/
  *중간조립품/박스포장 등). (2) 도면 뷰어 기본 회전 90도→270도로 수정(방향이 반대였음).
  (3) 클릭유도 애니메이션 여러 차례 반복 끝에 확정: 일반 버튼은 은은한 box-shadow 링
  펄스(스케일 애니메이션 전부 제거 — "멀미난다"는 명시적 피드백), 카드형 메뉴(action-card/
  step)는 회전 테두리 빛, 펼치기/접기 화살표는 방향별(▶좌우/▼상하) 상시 흔들림, 리스트
  호버색은 테마색 기반 통일. 회전 효과에 mask-composite를 썼다가 실기기(Whale 브라우저)
  에서 안 먹혀 혜성모양 얼룩 버그 남 — 화면 녹화로 발견, box-shadow 방식으로 교체.
  (4) reuse-scout 화면 통일성 점검 → 보안 결함 2건 수정(history_delete_selected 백엔드
  admin 강제 누락, /returns 권한가드 누락) + UX 통일 3건(계측기 검색, 검사대기 선택삭제
  권한게이트, NCR/반품/불량이력 도면보기 버튼). (5) 메모리/인수인계 체계 정비 — 개인
  auto-memory에 3건 신규 저장(시각검증 습관/애니메이션취향/영상프레임추출법), 글로벌
  PreCompact 훅(대화압축 직전 메모리 점검 알림) + 프로젝트 PostToolUse 훅(git commit
  직후 PROGRESS.md·메모리 점검 알림) 추가, CLAUDE.md 13절에 서브에이전트가 "사용자 관련
  교훈" 발견 시 보고서에 `💾 기억할 만한 내용:`으로 표시하는 규칙 추가.
- **2026-09-07**: 업체 관리(suppliers) 화면에 검색 기능 + 담당자 역할별(영업/품질/구매/기타) 다중 등록 기능 추가. `supplier_contacts` 테이블 신설, 기존 37개 업체 데이터 이관 완료(role="기타"로, 여러 명 섞인 이름은 자동분리 안 함). NCR·업체 성적표 발송 시 문서 종류에 맞는 담당자 이메일이 자동으로 채워지도록 연결(`get_default_contact`). 이후 디자인 에이전트가 두 화면 시각적 개선(카드 구분, 역할 배지 색상, 태블릿 반응형 여백) 완료. 커밋 9f90399.
- **2026-09-06 이전**: (report-generation.md 참고) 성적서+기준서(SAM) 통합 출력, 로고 삽입, 저장 경로·파일명 체계 전면 개편, 검사대기목록 검색 기능, 계정 프로필 수정 기능 등 완료

## 진행 중 / 보류 중

(CLAUDE.md 9절이 정본 — 여기는 그 요약만)

- **자유 양식 성적서**(드래그앤드롭 커스텀 템플릿 제작기) — 설계안은 있으나 미구현, 보류 중. 다음에 요청 들어오면 CLAUDE.md 9절 설계안대로 진행
- 성적표 일부 레거시 데이터 인코딩 깨짐(`supplier_reports.supplier`) — 원인 미조사, 지금 기능엔 영향 없어서 보류
- 담당자 구분 기능 중 "③용도별 발송 자동화"는 role 우선순위 방식으로 기본 반영됨 — 더 세밀한 제어(예: 담당자별 알림 옵션)가 필요해지면 그때 확장

## 참고: 성적서 자동화의 주요 기능은 이미 대부분 구현되어 있음

CLAUDE.md 3~8절 기준으로 NCR(부적합 통보서), 업체 월간 성적표, 품질현황 대시보드/집계
(`quality_report()`), 4M 변경점 추적, 성적서 위변조 검증(content_hash) 등은 **이미 구현
완료된 상태**다. 새 작업을 시작하기 전에 "혹시 이미 있는 기능은 아닌지" CLAUDE.md
3~8절을 먼저 확인할 것 — 없는 걸 새로 만드는 것보다 기존 기능 확장이 맞는 경우가 많다.

## QMS 서브에이전트 체계 관련 메모

- planner·developer·designer·quality-watcher·reuse-scout 5개 에이전트 전부 설계·실전검증 완료(2026-09-07)
- designer의 **iqc-app 전체 화면 최초 감사**는 아직 안 함 — 쿼터 넉넉할 때 진행 예정
- 서브에이전트는 매번 호출될 때 이전 작업을 기억하지 못한다(독립 컨텍스트) — 이 CLAUDE.md·PROGRESS.md가 그 간극을 메우는 용도
